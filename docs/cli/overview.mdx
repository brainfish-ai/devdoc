---
title: CLI Overview
description: Command line interface for managing your DevDoc documentation
---

The DevDoc CLI helps you develop, build, and deploy your documentation from the command line.

## Usage

Use the CLI with npx (recommended - no installation required):

```bash
npx @brainfish-ai/devdoc <command>
```

Or install globally:

```bash
npm install -g @brainfish-ai/devdoc
devdoc <command>
```

## Commands

| Command | Description |
|---------|-------------|
| `npx @brainfish-ai/devdoc create` | Create a new documentation project |
| `npx @brainfish-ai/devdoc dev` | Start development server with hot reload |
| `npx @brainfish-ai/devdoc build` | Build static site for production |
| `npx @brainfish-ai/devdoc deploy` | Deploy to DevDoc hosting |
| `npx @brainfish-ai/devdoc check` | Validate docs.json and content files |
| `npx @brainfish-ai/devdoc ai` | Set up AI agent configuration |
| `npx @brainfish-ai/devdoc keys` | Manage API keys for your project |
| `npx @brainfish-ai/devdoc domain` | Manage custom domains |
| `npx @brainfish-ai/devdoc whoami` | Show current project information |

## Global Options

These options work with all commands:

| Option | Description |
|--------|-------------|
| `--help` | Show help for a command |
| `--version` | Show CLI version |
| `--verbose` | Enable verbose logging |

## Configuration

The CLI looks for configuration in this order:

1. `docs.json` in the current directory
2. `DEVDOC_PROJECT_ROOT` environment variable
3. Parent directories (walks up the tree)

## Environment Variables

| Variable | Description |
|----------|-------------|
| `DEVDOC_API_KEY` | API key for deployments (CI/CD) |
| `DEVDOC_PROJECT_ROOT` | Path to project root |

## Quick Reference

```bash
# Create a new project
npx @brainfish-ai/devdoc create my-docs

# Create with specific template
npx @brainfish-ai/devdoc create my-docs --template openapi

# Start local development
npx @brainfish-ai/devdoc dev

# Deploy to production
npx @brainfish-ai/devdoc deploy

# Check for errors
npx @brainfish-ai/devdoc check

# Set up AI agents
npx @brainfish-ai/devdoc ai

# View project info
npx @brainfish-ai/devdoc whoami

# Regenerate API key
npx @brainfish-ai/devdoc keys regenerate

# Add custom domain
npx @brainfish-ai/devdoc domain add docs.example.com

# Check domain status
npx @brainfish-ai/devdoc domain status
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Create Project" icon="plus" href="/cli/create">
    Create a new documentation site.
  </Card>
  <Card title="Deploy" icon="cloud-arrow-up" href="/cli/deploy">
    Deploy your documentation.
  </Card>
  <Card title="Development" icon="play" href="/cli/dev">
    Local development server.
  </Card>
  <Card title="AI Agents" icon="robot" href="/cli/ai">
    Set up AI-powered documentation.
  </Card>
</CardGroup>
